import LookupManagement from './LookupManagement';

export default function StoreList() {
  return (
    <LookupManagement
      type="stores"
      title="Stores"
      description="Manage stores where ingredients can be purchased."
    />
  );
}
